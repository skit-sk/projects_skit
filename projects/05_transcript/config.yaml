defaults:
  chain: "one-pass-gpt4o-audio"
  model: null
  language: "ru"
  max_audio_minutes: 30
  max_cost_per_min: 0.01

output:
  dir_tg: null
  dir_tmp: "/tmp/out_transcript"
  pattern: "{topic}_{uid}_{chain}_{model_short}.md"

logging:
  file: "/tmp/opencode/transcript.log"

audio:
  format: "mp3"
  quality: 0
  out_dir: "/tmp"
