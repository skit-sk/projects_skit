// v2.0: Загрузка контрагентов из XML (УТ 10.3 → УТ 11.5)
// МОДУЛЬ ОБЪЕКТА
// Совместимость: УТ 11.5 (управляемые формы)
// Дата: 2026-07-27

// БСП: регистрация для подсистемы "Дополнительные отчеты и обработки"
Функция СведенияОВнешнейОбработке() Экспорт
	Параметры = Новый Структура;
	Параметры.Вставить("Вид", "ДополнительнаяОбработка");
	Параметры.Вставить("Наименование", "Загрузка контрагентов из XML v2.0");
	Параметры.Вставить("Версия", "2.0");
	Параметры.Вставить("БезопасныйРежим", Ложь);
	Параметры.Вставить("Информация", "Загрузка контрагентов и договоров из XML (УТ 10.3 → УТ 11.5)");

	Команда = Новый Структура;
	Команда.Вставить("Представление", "Загрузить контрагентов из XML");
	Команда.Вставить("Идентификатор", "ЗагрузитьКонтрагентовИзXML");
	Команда.Вставить("Использование", "ОткрытиеФормы");
	Команда.Вставить("ПоказыватьОповещение", Истина);
	Команда.Вставить("Модификатор", "");

	Параметры.Вставить("Команды", Новый Массив);
	Параметры.Команды.Добавить(Команда);

	Возврат Параметры;
КонецФункции
